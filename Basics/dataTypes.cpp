#include<iostream>
using namespace std;

int main() {
    int number = 5.3;
    double price = 90000;
    float interestRate = 3.78f;
    long fileSize = 9000L;
    bool isValid = false;
    char chr = 'a';

    int num {}; 
    /* will give us 0 as initializer
    if we put 8.9 in there it will not compile 
    if we put int itself then it will initialize*/ 
    cout << num;
    return 0;
}